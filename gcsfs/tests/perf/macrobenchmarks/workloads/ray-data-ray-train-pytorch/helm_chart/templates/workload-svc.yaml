apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  namespace: "{{ .Values.namespace }}"
spec:
  clusterIP: None
  selector:
    jobset.sigs.k8s.io/jobset-name: "{{ .Release.Name }}"
  ports:
  - name: ray
    port: 6379
    protocol: TCP
