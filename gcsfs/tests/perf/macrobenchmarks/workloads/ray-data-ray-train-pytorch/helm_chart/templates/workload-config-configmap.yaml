# yamllint disable
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-config"
data:
  workload-configuration: |-
{{ .Files.Get .Values.workload.configFile | nindent 4 }}
  metric_logging.py: |-
{{ .Files.Get "metric_logging.py" | nindent 4 }}
  requirements.txt: |-
{{ .Files.Get "requirements.txt" | nindent 4 }}
  requirements-cpu.txt: |-
{{ .Files.Get "requirements-cpu.txt" | nindent 4 }}
